import React from 'react';
import { Column } from './Column';

export default {
    title: 'Components/Column',
    component: Column,
    tags: ['autodocs'],
    parameters: {
        layout: 'fullscreen',
    },
};

export const Default = () => <Column />;